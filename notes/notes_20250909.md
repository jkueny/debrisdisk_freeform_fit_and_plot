### RDI mode

+ Using an optimized simple disk model
  + Overfits sharp features, putting higher frequencies in the reference spectrum.
  + Not so good for the RDI data.
    + See opt_run_0030 to opt_run_0046 for RDI i-band
+ Using a reference disk model using literature values
  + SPHERE H2 band results from Chen et al. (2020)
  + See opt_run_0047 to
